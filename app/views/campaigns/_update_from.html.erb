<script src="/ckeditor/ckeditor.js"></script>
<hr class="mb-5">
<div class="media px-3">
  <img class="img-fluid rounded-circle mr-3" src="<%= @campaign.user.authorizations.present? ? "https://graph.facebook.com/#{@campaign.user.authorizations[0].uid}/picture" : "https://help.lebara.com/assets/images/fdecb4bb.profile.jpg"%>" alt="" style="width: 50px">
  <div class="media-body">
    <%= form_for campaign_update, :url => campaign_updates_path(campaign_update), :method => :post do |f| %>
  		<%= f.hidden_field :campaign_id, :value => @campaign.id%>
      <%= f.text_field(:campaign_title, :class => "form-control", :placeholder => "標題")%>
      <%= f.text_area(:campaign_content, :class => "form-control")%>
      <script>
        CKEDITOR.replace( 'campaign_update_campaign_content' );
      </script>
      <button class="btn btn-outline-primary">送出</button>
    <% end %>
  </div>
</div>